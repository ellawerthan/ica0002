---
  - name: Setup
    hosts: all
    tasks:
      - setup:
  
  - name: Install MySQL exporter
    hosts: db_servers
    become: yes
    roles:
      - mysql_exporter

  - name: Fix Nginx node exporter proxies
    hosts: all
    become: yes
    roles:
      - node_exporter