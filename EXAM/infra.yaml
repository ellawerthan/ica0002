---
  - name: Setup
    hosts: all
    tasks:
      - setup:

  - name: Users
    hosts: all
    become: yes
    roles:
      - users
    tags:
      - users
  
  - name: Mysql
    hosts: application
    become: yes
    roles:
      - mysql
      - mysql_exporter
    tags:
      - mysql


  - name: Bind
    hosts: dns_servers
    become: yes
    roles:
      - bind
    tags:
      - bind